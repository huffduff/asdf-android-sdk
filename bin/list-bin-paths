#!/usr/bin/env bash

set -euo pipefail

BUILD_TOOLS=`ls ${ASDF_INSTALL_PATH}/build-tools | sort -n | tail -1`

echo "cmdline-tools/${ASDF_INSTALL_VERSION}/bin build-tools/${BUILD_TOOLS} platform-tools emulator ndk-bundle"
